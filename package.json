{
  "name": "dsh-mobile",
  "private": true,
  "type": "module",
  "scripts": {
    "sync-protocol": "node scripts/sync-protocol.mjs",
    "sync-protocol:check": "node scripts/sync-protocol.mjs --check",
    "build": "pnpm -r --filter @dsh-mobile/core --filter @dsh-mobile/protocol run build",
    "test": "pnpm -r --filter @dsh-mobile/core --filter @dsh-mobile/protocol run test",
    "typecheck": "pnpm -r --filter @dsh-mobile/core --filter @dsh-mobile/protocol run typecheck"
  },
  "devDependencies": {
    "nats": "^2.29.3",
    "typescript": "^5.9.0"
  }
}
